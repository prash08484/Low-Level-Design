/*
    The Observer Pattern is a behavioral design pattern where one object (Subject) maintains
    a list of dependent objects (Observers) and automatically notifies them whenever its state 
    changes.

    Instead of every object checking continuously for changes (polling), the Subject
    pushes updates to interested Observers.



*/

class ObserverPattern{

    // Violations 
    




    // Following



}